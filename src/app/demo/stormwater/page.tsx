import { StormwaterClient } from "./client";

export const metadata = { title: "Stormwater Outfall + Water Quality | EcoHeart × Hollywood" };

export default function Page() {
  return <StormwaterClient />;
}
