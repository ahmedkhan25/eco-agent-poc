import { HurricaneClient } from "./client";

export const metadata = {
  title: "Hurricane After-Action Playbook | EcoHeart × Hollywood",
};

export default function Page() {
  return <HurricaneClient />;
}
