import { TreeCanopyClient } from "./client";

export const metadata = { title: "Tree Canopy & Heat Island | EcoHeart × Hollywood" };

export default function Page() {
  return <TreeCanopyClient />;
}
